# rate-limiting-api
This project uses Node.js and Express.js to add rate limiting to an API for better security. It limits how many requests a user can make in a certain time using express-rate-limit, helping prevent abuse and keeping the server stable and reliable.
